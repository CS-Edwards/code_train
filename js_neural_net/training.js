//used in the video in Processing (Java) code but not p5 js code
//omit for now; looks like we do trianing in the sketch.js instead


class Point{
    constructor(){
        this.x ;
        this.y;
        this.label;
    }


}